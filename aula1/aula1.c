#include <stdio.h>

int main() {
    int numx = 10, numy = 4;

    int soma = numx + numy;

    printf("Hello, World!\n");

    printf("Esse número é: %i\n", numx);

    printf("A soma de %i e %i é igual a %i\n", numx, numy, soma);
    return 0;
}